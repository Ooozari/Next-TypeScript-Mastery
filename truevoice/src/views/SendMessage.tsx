import React from 'react'

interface Props {}

function SendMessage(props: Props) {
    const {} = props

    return (
        <>
        THis is send message page that will contain the form with text area input and buttin send message and other styling a user profile section showing status that is user accesption message or not </>
    )
}

export default SendMessage
